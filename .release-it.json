{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true,
    "push": true
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "before:init": [
      "node -e \"if (process.env.SKIP_TESTS !== 'true') { require('child_process').execSync('npm run test', {stdio:'inherit'}) } else { console.log('⏭️  Skipping tests (already passed on DEV branch)') }\""
    ]
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  }
}

